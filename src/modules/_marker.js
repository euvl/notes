/*
const makeMarker = () => {
  var marker = document.createElement('div')
  marker.style.cssText = 'color: #FFDD67; text-align: center; pointer-events: none;'
  marker.innerHTML = '●'
  return marker
}
*/

  //  editor.on('gutterClick', (cm, n) => {
  //    let info = cm.lineInfo(n)
  //    let marker = info.gutterMarkers ? null : makeMarker()
  //
  //    cm.setGutterMarker(n, 'breakpoints', marker)
//    })
